{
  "extends": "next/core-web-vitals",
  "root":true,
  "rules": {
    "@next/next/no-img-element": "off", // Disable the warning for using <img> instead of <Image>
    "react/no-unescaped-entities": "off", // Ensure JSX special characters are escaped
    "react-hooks/exhaustive-deps": "off" // Warn for missing dependencies in useEffect
  }
}
